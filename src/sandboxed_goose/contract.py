"""Public MCP contract shared by every framework adapter."""

SERVER_NAME = "sandboxed-goose"
SERVER_INSTRUCTIONS = (
    "Use calculate for basic arithmetic and sandbox_status to inspect the scaffold. "
    "Filesystem and shell execution are unavailable."
)
